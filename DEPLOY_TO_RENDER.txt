🚀 DEPLOY TO RENDER - EXACT STEPS

=== STEP 1: CREATE RENDER ACCOUNT (2 minutes) ===

1. Go to: https://render.com
2. Click "Get Started" or "Sign Up"
3. Sign up with GitHub (easiest)
4. Verify your email

=== STEP 2: PUSH CODE TO GITHUB (3 minutes) ===

Open Terminal and run these commands:

cd "/Users/apple/dbms project"
git init
git add .
git commit -m "Book Marketplace - Ready for deployment"

Then:
- Go to https://github.com/new
- Create repository: "book-marketplace"
- Copy the commands shown and run them

OR if you have GitHub CLI:
brew install gh
gh auth login
gh repo create book-marketplace --public --source=. --push

=== STEP 3: DEPLOY BACKEND (5 minutes) ===

1. Go to https://dashboard.render.com
2. Click "New +" → "Web Service"
3. Click "Connect GitHub" (if not connected)
4. Select "book-marketplace" repository
5. Fill in:
   - Name: book-marketplace-backend
   - Root Directory: backend
   - Environment: Node
   - Build Command: npm install
   - Start Command: npm start
6. Click "Advanced" → Add Environment Variables:
   
   MONGODB_URI = mongodb+srv://YOUR_MONGODB_CONNECTION_STRING
   JWT_SECRET = your_random_secret_key_12345
   JWT_REFRESH_SECRET = another_random_secret_67890
   CLIENT_URL = *
   PORT = 5001
   NODE_ENV = production

7. Click "Create Web Service"
8. Wait 5 minutes for deployment
9. COPY THE URL (e.g., https://book-marketplace-backend.onrender.com)

=== STEP 4: DEPLOY FRONTEND (5 minutes) ===

1. Click "New +" → "Static Site"
2. Select "book-marketplace" repository
3. Fill in:
   - Name: book-marketplace-frontend
   - Root Directory: frontend
   - Build Command: npm install && npm run build
   - Publish Directory: build
4. Click "Advanced" → Add Environment Variables:
   
   REACT_APP_API_URL = https://YOUR_BACKEND_URL.onrender.com/api
   REACT_APP_SOCKET_URL = https://YOUR_BACKEND_URL.onrender.com

5. Click "Create Static Site"
6. Wait 5 minutes for deployment

=== STEP 5: GET MONGODB (FREE) ===

If you don't have MongoDB:
1. Go to https://mongodb.com/cloud/atlas
2. Sign up (free)
3. Create free cluster (M0)
4. Create database user
5. Whitelist all IPs (0.0.0.0/0)
6. Get connection string
7. Add it to Render backend environment variables

=== DONE! ===

Your app will be live at:
Frontend: https://book-marketplace-frontend.onrender.com
Backend: https://book-marketplace-backend.onrender.com

Total time: 15 minutes
Cost: $0 (FREE!)

=== IMPORTANT NOTES ===

- Free tier sleeps after 15 min of inactivity
- First request after sleep takes 30 seconds
- Upgrade to paid ($7/month) for always-on
- MongoDB Atlas free tier: 512MB storage

=== IF YOU GET STUCK ===

1. Check build logs in Render dashboard
2. Make sure GitHub repo is public
3. Verify environment variables are correct
4. MongoDB connection string must allow all IPs

=== QUICK LINKS ===

Render: https://render.com
MongoDB: https://mongodb.com/cloud/atlas
GitHub: https://github.com/new
